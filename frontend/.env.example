# Only needed for production builds (Vercel/Netlify/etc). Leave unset for
# local dev — Vite's dev server proxy (vite.config.js) handles /api locally.
VITE_API_URL=https://your-backend-host.onrender.com/api
